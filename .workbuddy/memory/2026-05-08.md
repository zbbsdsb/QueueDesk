# 2026-05-08 Daily Notes

## 重构与优化（继续推进）

### 已修复
- `admin/users/page.tsx`：修复 JSX 语法错误（缺失 `]`），lint 零错误 ✅
- `agent/dashboard`："New Ticket" 按钮链接从 `/app/new` 改为 `/agent/tickets/new`
- `agent/TicketsTable.tsx`：同上修复链接
- `agent/dashboard` 空状态 "Create one" 链接同步修复

### 新增功能
- **`/agent/tickets/new` 页面**：Agent 专用新建工单页面
  - 支持搜索选择 requester（防抖 300ms）
  - 选择队列、优先级、填写主题/描述
  - 提交后跳转至工单详情页

### 中央化配置（P2-2）
- `lib/types.ts` 新增：
  - `QUEUE_COLORS`：队列饼图颜色数组
  - `SLA_STATUS_COLORS`：SLA 状态颜色配置
- `agent/dashboard/page.tsx`：删除本地 `QUEUE_COLORS`，改为从 `lib/types` 导入

### Schema Migration
- 创建 `supabase/migrations/001_initial_schema.sql`
  - 9 张表：tenant、app_user、team、queue、sla_policy、ticket、contact、ticket_comment、invite
  - 软删除（`deleted_at`）、RLS 策略骨架、`updated_at` 触发器
  - 部分索引：`idx_ticket_active`（仅活跃工单）

### 设计系统（P2-4）
- 重写 `src/app/globals.css`
  - 完整的 `@theme inline` token 系统（品牌色、表面色、墨水色、状态色、阴影、圆角）
  - 深色模式变量（`.dark` 类切换）
  - 滚动条美化、`focus-visible` 统一样式、`::selection` 颜色

### 任务状态
- ✅ P0-2: migration 文件
- ✅ P1-1: AgentShell 已用 `useAuth()`（发现之前摘要有误，代码已完成）
- ✅ P1-2: 修复 New Ticket 链接（3 处）
- ✅ P1-3: AgentShell 已有退出登录按钮（同上，代码已完成）
- ✅ P2-2b: `QUEUE_COLORS` 中央化
- ✅ P2-4: `globals.css` 设计系统
- ⏳ P2-3: `ticket_no` 字段（待做，需 migration + 触发器）
